# Car Incident Logger — Python dependencies
# Install with: pip install -r requirements.txt

# Core computer vision
opencv-python>=4.8.0

# Local Whisper transcription (CTranslate2 backend — fast + low memory)
faster-whisper>=0.10.0

# Audio capture
sounddevice>=0.4.6

# Numerical computing (used throughout)
numpy>=1.24.0

# Configuration
pyyaml>=6.0

# GPS dongle support (direct NMEA serial fallback when gpsd is unavailable)
pyserial>=3.5
pynmea2>=1.19.0

# Keyboard button listener
pynput>=1.7.6

# Web UI
flask>=3.0

# ── Optional ──────────────────────────────────────────────────────────────────
# Alert chime playback (only needed if notifier.chime_enabled = true)
# playsound>=1.3.0

# Phase 2 ALPR
ultralytics>=8.0.0      # YOLOv8 vehicle + plate detector
fast-plate-ocr>=0.2.0   # ONNX OCR optimized for NA plates (replaces EasyOCR/PaddleOCR)

# Raspberry Pi GPIO (install only on Pi hardware)
# RPi.GPIO>=0.7.1
